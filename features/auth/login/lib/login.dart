import 'package:datastore/provider/session/session_providerimpl.dart';


/// A Calculator.
class Calculator {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;
  // var session = DevSessionProviderimpl();
}
